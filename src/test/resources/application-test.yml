spring:
  datasource:
    url: jdbc:h2:mem:greenshield;MODE=MySQL;DB_CLOSE_DELAY=-1;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
    driver-class-name: org.h2.Driver
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false

app:
  admin-seed:
    enabled: false
  material-zone-seed:
    enabled: false
  product-seed:
    enabled: false

cloudinary:
  cloud-name:
